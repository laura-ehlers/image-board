import {NavBarItemComponent} from "./nav-bar-item.component";

describe('NavBarItemComponent', () => {
  it('should initialize NavBarItem component', function () {
    let comp = new NavBarItemComponent();
    expect(comp).toBeTruthy();
  });
})

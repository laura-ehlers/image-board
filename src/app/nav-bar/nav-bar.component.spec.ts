import {NavBarComponent} from "./nav-bar.component";

describe('NavBarComponent', () => {
  it('should initialize NavBar component', function () {
    let comp = new NavBarComponent();
    expect(comp).toBeTruthy();
  });
})

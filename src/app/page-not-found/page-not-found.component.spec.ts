import {PageNotFoundComponent} from "./page-not-found.component";

describe('PageNotFound', () => {
  it('should initialize PageNotFound component', function () {
    let comp = new PageNotFoundComponent();
    expect(comp).toBeTruthy();
  });
})
